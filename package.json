{
  "name": "@emdash-seo/toolkit",
  "version": "0.1.0",
  "description": "SEO toolkit for Emdash CMS — meta tags, OpenGraph, JSON-LD, sitemaps, and content analysis",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "npx oxlint src/ test/"
  },
  "peerDependencies": {
    "emdash": "^0.1.0"
  },
  "devDependencies": {
    "emdash": "^0.1.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2",
    "zod": "^4.3.6"
  }
}
